name: Starter Workflow Demo
on: [pull_request]
  
jobs:
  setup:
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v3
    - name: demo workflow job step 1
      run: echo This is a demo start workflow 1
 
  build:
    runs-on: ubuntu-latest
    needs: setup
    
    steps: 
    - uses: actions/checkout@v3
    - name: demo workflow job Step 2
      run: echo This is Demo 2
